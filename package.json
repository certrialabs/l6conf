{
  "name": "l6conf",
  "version": "0.1.0",
  "description": "This node.js library introduces a policy for transforming environment variables into a hierarchically structured json and allows you to overwrite configurations by importing multiple config files consequently.",
  "main": "./lib/l6conf.js",
  "keywords": [ "configuration", "hierarchy", "nested env", "key value store", "multiple configurations", "environment config", "json config" ],
  "author": "Krasimir Stavrev <kstavrev@elsix.bg>",
  "repository": {
    "type": "git",
    "url": "https://github.com/elsix/l6conf.git"
  },
  "license": "MIT",
  "dependencies": {
    "lodash": "^4.11.1"
  },
  "devDependencies": {
    "chai": "^3.5.0"
  },
  "engines": {
    "node": ">= 4.0.0"
  }
}
